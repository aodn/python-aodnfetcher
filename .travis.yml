language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"

install:
  - pip install -r test_requirements.txt
  - pip install pytest-cov
  - pip install codecov

script:
  - pytest --cov=./

after_success:
  - codecov
